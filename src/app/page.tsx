import { Container } from "@/components/ui/container";

export default function Home() {
  return (
    <main className="flex min-h-[calc(100vh-4rem)] flex-col items-center">
      <Container>
        <div className="py-12">
          {/* Content will be added later - navbar is in layout.tsx */}
        </div>
      </Container>
    </main>
  );
}
